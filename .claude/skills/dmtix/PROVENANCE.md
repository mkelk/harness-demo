Vendored from the user-level skill `~/.claude/skills/dmtix/` on 2026-09-17. Upstream:
https://github.com/mkelk/skills → `skills/dmtix/` (commit 1f16464, 2026-08-18). The three doors
(`bootstrap`, `start`, `go`) resolve the ticks engine at project level first, so this repo
runs with the copies under `.claude/skills/` and nothing from the user's home directory.
